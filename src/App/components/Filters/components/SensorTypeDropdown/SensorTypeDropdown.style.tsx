import styled from 'styled-components';
import { Dropdown } from 'semantic-ui-react';

const StyledSensorTypeDropdown = styled(Dropdown)`
  &.ui.dropdown {
    height: 100%;
    width: 300px;
  }
`;

export default StyledSensorTypeDropdown;
