import styled from 'styled-components';
import { Form } from 'semantic-ui-react';

const StyledNewEntryForm = styled(Form)`
  width: 100%;
  height: 100px;
  padding-top: 10px;
`;

export default StyledNewEntryForm;
